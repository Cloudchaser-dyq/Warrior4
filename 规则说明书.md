# 《魔兽世界》4AC — 规则说明书

> 本文档说明该可视化工具体系背后的**魔兽争霸模拟规则**(对应《C++ 程序设计·魔兽迷题》/北大在线评测 4AC 题),以及**如何使用本工具**进行战役可视化回放。

---

## 一、工具使用指南

### 1.1 运行方式
- **双击 `index.html`** —— 无需联网、无需安装,纯浏览器本地运行(Chrome / Edge / Firefox 均可)。
- 页面自动加载内置的 27 组标准样例数据(`data_embedded.js`),打开即可开始回放。

### 1.2 界面构成
| 区域 | 说明 |
|---|---|
| 顶部中央时钟 | 当前回放时刻 `时:分`(游戏内时间) |
| 顶部两侧 | 红方营地 / 蓝方营地(司令部),营地标高实时**生命元(HP 池)** |
| 中间道路 | `城1 ~ 城 n`;城多时(>10 座)自动**折为多行**,一行一排,蓝方营地位于最后一行末尾;城下显示该城**元素量**;城上显示该城**旗帜**(红⚑/蓝⚑) |
| 单位 | 图标 + 血条 + 名称标签(`兵种+编号`)+ 武器徽章 + 士气/忠诚 |
| 右侧侧栏 | 战况日志(与标准输出逐行对应) |
| 底部控制条 | 播放/暂停、步进、上一步、复位、跳尾、速度、进度条、案例切换 |

### 1.3 操作快捷键
| 键 | 功能 |
|---|---|
| `空格` | 播放 / 暂停 |
| `→` | 步进 1 分钟 |
| `←` | 回退 1 分钟 |

### 1.4 播放速度
`0.3× ~ 8×` 可选。默认 1×,每分钟游戏中时间约 0.1 秒。

> 说明:某些 Case(双方势均力敌)可能长时间无一方攻入对方营地,**会一直打到配置的 T 分钟**才结束,这是正常现象,不是卡死。多行布局下大战场也能完整显示,场景超高时可用鼠标滚轮滚动。

### 1.5 加载自己的数据
点击 **📂 打开数据**,选择**输出日志**(`.out` / `.txt` / `.log`)和(可选)**输入参数**(`.in`)。
- 只给日志也可以:程序会从日志中自动推断城池数 `n` 等参数;
- 同时给 `Warcraft.in` 时,可在顶栏看到该 Case 的完整参数(m/n/r/k/T、生命值、攻击力)。

### 1.6 事件特效对照
| 事件 | 场景表现 |
|---|---|
| 出生 | 单位从营地弹出(spawn 动画),日志 `born` |
| 行军 | 单位沿道路平滑滑行,日志 `marched to city` |
| 放箭 | 射程动画,命中则目标显示「中箭身亡」,日志 `shot` / `shot and killed` |
| 炸弹 | ⚑爆炸特效,双方同归于尽浮字,日志 `used a bomb` |
| 战斗 | 刀光闪烁,`attacked` / `fought back` / `was killed` |
| 获元 | 城上方「＋N 生命元」浮字 |
| 升旗 | 城上旗帜变红/蓝 |
| 龙吼 | 士气>0.8 的龙获胜后怒吼,日志 `yelled` |
| 狮子逃跑 | 「🦁 狮子逃跑」浮字 |
| 占领营地 | 营地标注「被占领」并触发战斗结束 |

---

## 二、角色与属性

### 2.1 基地(司令部)
- 红方 N0 位于地图最左,蓝方 HQ 位于最右。
- 初始各有 **M 点生命元(Element)**。
- 生命元用于生产武士;生产消耗对应武士的生命值。

### 2.2 五种武士(默认属性由输入决定,不同 Case 不同)
| 武士 | 生命值 | 攻击力 | 特性 |
|---|---|---|---|
| **Dragon 龙** | HP1 | F1 | 士气值 **morale**;获胜 +0.2、失败 -0.2(平局 -0.2);当士气>0.8 时获胜后**怒吼** |
| **Ninja 忍者** | HP2 | F2 | 拥有 2 件武器(编号 mod 3 与 +1);**不反击** |
| **Iceman 冰人** | HP3 | F3 | **两格减血**:每行 2 步,hp 减 9(若不足 9 则置 1),攻击力 +20 |
| **Lion 狮** | HP4 | F4 | **忠诚值 loyalty**;忠诚 ≤0 时**逃跑**;狮子杀死敌方可**吸血**(获得敌方死亡前血量) |
| **Wolf 狼** | HP5 | F5 | 击杀敌人后**缴获**对方武器(缺哪种补哪种) |

> 生产顺序(每回合从对应序列依次轮询):红方 `iceman, lion, wolf, ninja, dragon`;蓝方 `lion, dragon, ninja, iceman, wolf`。
>
> 红武士第 1 个 = 龙命{dragon}…,编号从 1 开始每方独立。

### 2.3 三种武器
| 武器 | 效果 | 耐久/消耗 |
|---|---|---|
| **Sword 剑** | 攻击力 = 武士攻击力 × 20%;**每次使用后衰减为 80%**,直到 0 自动消失 | 随使用次数衰减 |
| **Bomb 炸弹** | 双方同归于尽(**战斗中先判定攻方使用**),一次性 | 立即摧毁持有者与目标 |
| **Arrow 箭** | 射程 2 城,对目标造成 **r 点伤害**(35 分钟发动);若目标 hp≤0 则射杀 | 限用 **3 次**,用完消失 |

---

## 三、地图与旗

- 地图:红 HQ(城 0)— 城 1 … 城 n — 蓝 HQ(城 n+1)。
- 旗(Flag):城被某方连续占领 2 次后,该城**立旗**;立旗后**旗主方获得先手**(即便攻守顺序因此反转)。
- 城价值:每整小时的第 20 分钟,每城产出 **10 生命元**(累积,直到被取走)。夺取后归零并记入占领方。

---

## 四、时间轴(每小时的分钟事件)

| 时刻 | 事件 |
|---|---|
| **:00** | 红、蓝各生产武士(按序列,若生命元不足停止生产) |
| **:05** | 狮子忠诚 ≤0 → **逃跑**,离开地图 |
| **:10** | 全军**行军一步**(红向右、蓝向左);到营地触发**占领判定**;冰人结算 debuff |
| **:20** | 每座城产出 +10 生命元 |
| **:30** | 独占城的武士把该城元素上缴己方营地(归零) |
| **:35** | **放箭**(携带箭且攻击范围内有敌人的武士),伤害 r,耗尽射杀 |
| **:38** | **炸弹判定**(同城双方,若攻方预测必死则引爆同归于尽) |
| **:40** | **战斗**(同城双方对打;胜方获城元素,连续 2 胜 → 立旗;营地给获胜在场武士 +8hp,若营地生命元≥8) |
| **:50** | 营地报告生命元 |
| **:55** | 所有武士报告武器(按 `arrow,bomb,sword` 顺序),无武器则报 `no weapon` |

---

## 五、战斗结算细节

1. **先手**决定:有旗者先手;无旗时按城序号 — 奇数为红先,偶数为蓝先。
2. 攻方造成伤害 = 自身攻击力 + 剑攻击力(若有)。
3. 守方若没死且不是忍者,**反击**:造成 攻方承受 守方攻击力一半 + 守方剑攻击力。
4. 死亡判定:hp≤0。
5. 胜方:
   - 得到该城元素(若胜方存活);
   - 若胜方是狼,缴获对方武器;
   - 若胜方是龙,士气 +0.2(吼叫需士气>0.8);
   - 营地对在场获胜武士奖励 **+8hp**(若营地生命元≥8)。
6. 平局:双方士气-0.2(龙)、忠诚-`k`(狮);城池连胜计数清零。
7. 连续占领 2 次 → 立旗。

---

## 六、胜负条件

- 任一营地被**对方武士到达** → 该营地被占领(**`XX:10 red/blue headquarter was taken`**)。
- 首个营地被夺即**战斗结束**(即使之前另有武士已进入并不重复结算)。

---

## 七、技术实现说明(给开发/维护者)

- **纯前端零依赖**:`index.html` + `style.css` + `parser.js`(日志解析)+ `engine.js`(状态机→快照)+ `render.js`(Canvas 场景 + DOM 单位与特效 + 回放控制器)+ `data_embedded.js`(内嵌样例)。
- **事件驱动回放**:以 `.out` 日志为唯一事实来源,事件→快照数组,播放时按索引推进;Cvoid应力求忠实于日志数值(hp/force/武器)。
- **本地文件加载**:`file://` 下 fetch 受限,故样例数据内嵌;用户自选文件不触发跨域问题。
- 需要**修改渲染节奏**或**添加新事件**:见 `render.js` 的 `eventText / playEffects`,事件类型枚举见 `parser.js`。

---

© 2026 · 魔兽 4AC 可视化工具